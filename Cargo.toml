[package]
name = "sagelib"
version = "0.1.0"
edition = "2024"
[lib]
crate-type = ["cdylib"]

[dependencies]
napi = { version = "2", default-features = false, features = ["napi4"] }
napi-derive = "2"
glob = "0.3"
regex = "1.10"

[build-dependencies]
napi-build = "2"

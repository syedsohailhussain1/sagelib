{
  "name": "sagelib",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "build": "npx @napi-rs/cli build --platform --release",
    "build:debug": "npx @napi-rs/cli build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^3.8.6"
  },
  "napi": {
    "name": "sagelib"
  }
}
